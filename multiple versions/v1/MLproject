name: v1

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      random_state: {type: int, default: 0}
      max_depth: {type: int, default: 3}
    command: "python Modelv1.py {random_state} {max_depth}"
